# ── Subscription backend (#2/#7) — see docs/SUBSCRIPTION_BACKEND_PLAN.md ──
# Copy to .env.local and fill in. NEVER commit real values.

# Postgres (Neon/Railway/Supabase). Required for auth + subscriptions.
DATABASE_URL="postgresql://user:pass@host:5432/db?sslmode=require"

# Stripe (Phase 3) — test keys to start
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
STRIPE_PRICE_PREMIUM_MONTHLY=""
STRIPE_PRICE_PREMIUM_YEARLY=""
STRIPE_PRICE_FAMILY_MONTHLY=""
STRIPE_PRICE_FAMILY_YEARLY=""

# Email (Phase 4) — gift-card delivery
RESEND_API_KEY=""
EMAIL_FROM="Noor-ul-Quran <noreply@your-domain.com>"

# App
NEXT_PUBLIC_SITE_URL="http://localhost:3100"
